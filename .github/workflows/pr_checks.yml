name: 'PR Validation'
on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  pr-validation:
    if: github.actor != 'dependabot[bot]'
    uses: maharshi-cd/github-shared-workflows/.github/workflows/pr_checks.yml@fix/pr-checks-workflow
    secrets: inherit
    with:
      subjectPattern: '^.+$'



